{
  "name": "querystring-number",
  "version": "1.1.1",
  "main": "cjs/index.js",
  "types": "lib/index.ts",
  "files": [
    "cjs",
    "lib"
  ],
  "license": "MIT",
  "scripts": {
    "build": "esbuild --define:process.env.NODE_ENV=\\\"production\\\" lib/index.ts --outdir=cjs --target=es6  --bundle --external:querystring-number --format=cjs --minify"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ymzuiku/querystring-number"
  },
  "devDependencies": {
    "typescript": "3.5.2"
  }
}